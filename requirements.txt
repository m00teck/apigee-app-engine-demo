fastapi==0.100.0   # Latest stable version of FastAPI
uvicorn==0.23.0   # ASGI server to run the app
gunicorn==20.1.0  # Production server for App Engine
python-multipart  # For handling form data (if needed)
pydantic==1.10.7  # Data validation and settings management